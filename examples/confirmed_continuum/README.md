# Synthetic confirmed-rule acceptance

These four rows are constructed numerical examples, not real Archive observations.
Row 0 uses guide example A. Rows 1–3 respectively change aggregate RMS to 0.21,
remove RMS, and move the candidate outside its half-power beam. A broad synthetic
footprint retains all four for criterion evaluation. See
[the CLI and expected results](../../docs/confirmed_continuum.md).

VOTable field structure follows the existing NGC6240 replay; scientific values,
identifiers, timestamps and endpoint are synthetic. No live acquisition is claimed.
The replay manifest pins all bytes. Do not edit a response without updating its
SHA-256 and reviewing the corresponding acceptance expectation.
