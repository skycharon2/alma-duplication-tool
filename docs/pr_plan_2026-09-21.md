# PR plan after the confirmed scientific workflow

Current increment baseline: `a3d2923` (PR #73; implementation `8bd931b`).
This is the single owner of remaining engineering tasks. Scientific confirmation is an input,
not a new precondition. Submit separate reviewable engineering increments.

| PR | Branch suggestion | Concrete changes | Exit gate |
| --- | --- | --- | --- |
| 1 — completed: `4122f81`, PR #70 / `82ffb01` | feat/archive-continuum-confirmed | Confirmation record; Archive POS-SINGLE; CONT-FREQ/CONT-RMS; confirmed ANGULAR/CONT-SETUP wrappers; continuum aggregation; CLI report; pairing reference contract | Offline guide A positive/negative/unknown, boundaries, full existing suite, real NGC6240 mapping replay |
| 1a — completed: `61813bd`, PR #71 / `c1785b2` | fix/continuum-contract-closure | Contribution references, Solar CLI exemption, selected-branch diagnostics, report v3 | 962 passed / 10 skipped; synthetic and real offline replay; no live/full external snapshot claim |
| 2 — completed: `7ab5981`, PR #72 / `3b6105b` | feat/archive-line-evidence-pairing | First-class redshift; explicit per-window planned resolution/RMS validation; versioned em_xel mode evidence; coherent pair builder | Counts 1/128/129/1920 and invalid/conflicting metadata; REST/SKY cases; crossed-SPW and multi-ASDM rejection; no Member-wide mode |
| 3 — completed: `8bd931b`, PR #73 / `a3d2923` | feat/archive-line-evaluation | LINE-FDM/COVERAGE/RESOLUTION/RMS; intermediate quantities; pair AND / context-local whole-pair OR; report v4, evaluation v5, validation v6 | Guide B; endpoints; coarser-resolution RMS blocked; deeper/equal/worse RMS; missing evidence; incomplete enumeration; no cross-SPW borrowing; independent simultaneous branches |
| 4a — implemented in this patch; pending review/merge | feat/acceptance-interface-contract | Pinned case catalog, independent raw-field reference ledger, report comparisons and gap inspection; synchronized form and thin-interface contract | Offline catalog passes; exact pair provenance checked; review status remains explicit; no real scientific labels invented |
| 4b — next | feat/assessment-thin-interface | Input/results/pair evidence/export UI consuming report v4 and the existing backend | UI and CLI agree; source completeness and unresolved reasons visible; no duplicated formulas; collect actual proposal cases and record independent review |

Do not merge the code merely because test count increases. Review the request,
fixture, criteria and branch result together. Keep real-source field/association
regression separate from numerical science acceptance. CASE1/2 remain retrieval
regressions; NGC6240 remains an engineering replay.

Queue source integration remains operational in PR1. New Queue scientific
mappings require their own follow-up increment; never apply Archive aggregate
RMS or em_xel contracts to Queue columns. Mosaic, moving targets, TP, arbitrary
bandwidth conversion, image achieved-RMS work and LLM decision logic stay outside
this series.

## Weekly report measures

- Retained and assessed candidate counts, separately by source.
- Evaluable/pass/fail/unknown counts for each criterion.
- Missing reason distribution and how many contexts each fix unblocks.
- Count of numerical acceptance cases versus independently reviewed real cases.
- Source status, retrieval strategy/scope and display truncation.
- Method versions in reports; no retrospective approval of historical outputs.
